Github password - Mounika@1918
remaining some are Vfy#04
         or
      Vivify#04     



              **Git commands**
  
     *git push*

first file manager lo new folder create cheyyali akada gitbash here chesi repository code copy chesi "git clone code" paste cheyyali

1)first check in which directory we are => ls

2)change directory(dir) to respective dir => cd dir name(ls kotaka vachey names(dir) lo manaki kavalisina name(dir))

3)git branch(shows the branch where we are)

4)if main(branch) => if 1st time checkout use this command => git checkout -b branchname
                  => already existing branch use this command => git checkout branchname

5)git status(it shows what modifications are done)

6)git add ./

7)git status(added files are shown in green color)

8)git commit -m "msg" (what changes are done that content should be written)

9)git push(if ask credentials then we have to give username and password of git)

10)git status(nothing to commit working tree clean illa vasthey git push ipoyeinatu)



     *git pull*

oka kotha folder lo git bash here cheyyali andhulo git clone cheyyali with respective code(repository url)
after succesfully cloned git pull origin branchname(andhulo nunchi (pull or code) kavallo aa branch name)